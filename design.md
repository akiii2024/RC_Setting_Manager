# RC Setting Manager デザインガイドライン

このドキュメントは、RC Setting Manager の画面を追加・変更するときの判断基準です。目的は、見た目を完全に固定することではなく、どの画面でも同じ優先順位で設計判断ができる状態を作ることです。

## 1. デザインの原則

### Technical, calm, focused

RC セッティングという情報量の多い作業を、落ち着いて正確に進められる UI にします。

- **Technical**: 数値、単位、車種、設定の関係が素早く読み取れる。
- **Calm**: 装飾や色を増やしすぎず、長時間使っても疲れにくい。
- **Focused**: 1 画面の主目的と主要アクションが明確である。

迷ったときは、次の順で優先します。

1. 誤操作を防げること
2. 情報の階層と状態が読み取れること
3. ライト／ダーク、端末サイズ、文字拡大に耐えること
4. 見た目の華やかさ

## 2. 唯一の実装基準

色、文字、標準コンポーネントの正本は [`lib/app/app_theme.dart`](lib/app/app_theme.dart) です。このドキュメントと実装が食い違う場合は、意図を確認したうえで両方を同じ変更で更新してください。

`images/` 配下のスクリーンショットには旧グリーン基調の UI が含まれます。レイアウトや機能の参考にはできますが、色、文字、角丸、影の基準にはしません。

新規 UI では以下を守ります。

- `Theme.of(context)` と `ColorScheme` を使い、ライト／ダーク双方に対応する。
- 既存の `ThemeData` で表現できる場合は、ローカルな `TextStyle`、色、形状を追加しない。
- 同じ見た目や振る舞いが 2 画面以上に現れる場合は、共通 Widget またはテーマへ昇格する。
- 例外値には「なぜテーマ値では不十分か」がコードから分かる名前またはコメントを付ける。

## 3. カラー

Material 3 のセマンティックカラーを使います。色は「見た目」ではなく「役割」で選びます。

| 役割 | Light | Dark | 用途 |
| --- | --- | --- | --- |
| `primary` | `#3F56C5` | `#BCC3FF` | 最重要アクション、フォーカス、選択の強調 |
| `secondary` | `#006B75` | `#82D3DD` | 補助的な強調 |
| `tertiary` | `#8B5000` | `#FFB86B` | 注意、別系統のデータ表現 |
| `surface` | `#F9F9FF` | `#11121A` | 画面背景 |
| `surfaceContainerLow` | `#F2F3FB` | `#191A23` | カード |
| `surfaceContainerHigh` | `#E6E8F2` | `#272831` | 入力欄、ダイアログなど前面要素 |
| `onSurface` | `#1A1B24` | `#E5E1EC` | 本文と主要情報 |
| `onSurfaceVariant` | `#454651` | `#C8C5D0` | 補助情報、アイコン |
| `outline` | `#767681` | `#918F9A` | 明確な境界 |
| `outlineVariant` | `#C6C6D1` | `#474651` | 控えめな境界 |
| `error` | `#BA1A1A` | `#FFB4AB` | エラー、破壊的操作 |

### カラーのルール

- 通常の本文は `onSurface`、補助文は `onSurfaceVariant` を使う。
- コンテナ上の文字・アイコンは、対応する `on...Container` を使う。
- 選択状態は色だけでなく、アイコン、ラベル、太さ、形状のいずれかも併用する。
- 成功・警告などテーマに未定義の状態色を直接 `Colors.green` 等で増やさない。複数画面で必要になった時点で `ThemeExtension` に集約する。
- 背景色の上にさらに白いカードを重ね続けない。Surface の段階差で階層を作る。
- 色の不透明度だけで無効状態を設計せず、Material の disabled state を優先する。

## 4. タイポグラフィ

- 見出し: **Space Grotesk**。`display*`、`headlineLarge/Medium`、`titleLarge` に使用。
- 本文・ラベル: **Inter**。
- 日本語フォールバック: **Noto Sans JP**。
- フォント定義は `AppTheme` を通し、Widget 内で `fontFamily` を指定しない。

文字サイズを直接指定せず、意味に合う `TextTheme` を選びます。

| スタイル | 用途 |
| --- | --- |
| `headlineMedium` / `headlineSmall` | ページ内の大きな見出し、主要な結果 |
| `titleLarge` | AppBar タイトル、カードの強い見出し |
| `titleMedium` | セクション名、リスト項目名、入力グループ名 |
| `bodyLarge` | 読ませる説明、主要な値 |
| `bodyMedium` | 標準本文、補足説明 |
| `bodySmall` | 日時、単位、メタデータ |
| `labelLarge` | ボタン、タブ、操作ラベル |
| `labelMedium` / `labelSmall` | 短い分類名、補助ラベル |

原則としてウェイトは通常本文 `w400`、小見出し `w600`、主要見出しと操作 `w700` です。強調のためだけに文字サイズを増やさず、まず階層、余白、ウェイトを使います。長い日本語を想定し、固定高のテキスト領域は避けます。

## 5. 余白とレイアウト

基本単位は **4 dp**、主要な間隔は次のスケールから選びます。

| Token | 値 | 主な用途 |
| --- | ---: | --- |
| `space-1` | 4 | アイコンと微小要素 |
| `space-2` | 8 | 密接な要素、リスト内の小間隔 |
| `space-3` | 12 | 関連要素 |
| `space-4` | 16 | 標準の画面端、カード内の最小余白 |
| `space-5` | 20 | セクション周辺、標準カード内余白 |
| `space-6` | 24 | 大きなカード、セクション間 |
| `space-8` | 32 | 大きな意味の区切り |

- モバイルの左右余白は原則 `16`。情報を絞ったヒーロー領域や大きなカード内部では `20` または `24`。
- スクロール画面は Bottom Navigation、FAB、Safe Area と重ならない下余白を確保する。ホーム系の現行基準は `120`。
- ページ本文は広い画面で際限なく伸ばさない。標準的な単一カラムは中央寄せ `maxWidth: 720` を基準とする。
- `LayoutBuilder` で内容幅に応じて判断し、端末名や向きだけで分岐しない。現行の compact 境界は `720`。
- 関連情報は近づけ、セクションの切り替わりには要素内余白より大きな余白を置く。

## 6. 形状、境界、影

形状は柔らかく、情報構造は明快にします。

- カード: `24` radius、`surfaceContainerLow`、薄い `outlineVariant`、原則 elevation `0`。
- 入力欄: `18` radius、`surfaceContainerHigh`。フォーカス時のみ primary の `2` dp 枠。
- リスト項目: `20` radius、最小高 `64`。
- ダイアログ: `28` radius。
- Bottom Sheet: 上辺 `32` radius。
- FAB: `20` radius。
- ボタン、Chip、Tooltip、選択インジケータ: Stadium 形状。

影で階層を表現しすぎず、Surface の段階と境界を優先します。FAB など「浮いて操作できる」要素だけに控えめな elevation を使います。角丸値を都度発明せず、上記の最も近い役割を選びます。

## 7. コンポーネント

### ボタン

- 画面の主要アクションは原則 1 つ。`FilledButton` または `ElevatedButton` を使う。
- 補助アクションは `OutlinedButton`、低優先の操作は `TextButton`。
- 破壊的操作は `error` を用い、実行前に対象と結果を確認できる文言を表示する。
- ボタンの最小高は `52`、アイコンだけの操作は最小 `48 x 48`。
- 「はい」「OK」より、`保存する`、`削除する` のように結果が分かる動詞を使う。

### カードとリスト

- カードは同じ目的の情報と操作をひとまとまりにする場合に使う。単なる余白や飾りのためにネストしない。
- タップ可能な領域はカード全体または行全体にし、視覚上の領域とヒット領域を一致させる。
- タイトル、主要値、補助情報、操作の順を維持する。
- 一覧内で同じ情報は常に同じ位置に置く。

### フォーム

- ラベルを hint だけで代用しない。入力後も項目名が分かる状態を保つ。
- 数値には単位を併記し、値と単位の視覚的な主従を付ける。
- 検証エラーは該当欄の近くに、原因と修正方法が分かる文章で表示する。
- 保存可能か、未保存か、処理中かを明示し、連続送信を防ぐ。
- 同じ種類の設定入力は同じ Widget と並び順を再利用する。

### ナビゲーション

- 主要領域の切り替えには `NavigationBar` を使い、ラベルを常時表示する。現行高は `80`。
- 戻る操作、ページタイトル、ページ固有アクションは AppBar に置く。現行高は `72`。
- FAB は現在のページ内容に対する主要な作成・実行アクションに限定する。
- Bottom Navigation と FAB で同じアクションを重複させない。

### ダイアログとフィードバック

- 確認だけならダイアログ、複数項目の選択や補助フローなら Bottom Sheet、独立した作業なら新しいページを使う。
- 成功や軽微な失敗は SnackBar、ユーザーが対応しないと進めない問題は画面内エラーまたはダイアログで伝える。
- 読み込み中は既存レイアウトを可能な限り維持し、処理が長い場合は何を待っているか示す。
- 空状態は「データがない」だけで終わらせず、次にできる主要アクションを 1 つ提示する。

## 8. アイコンとデータ表現

- Material Symbols / Flutter の `Icons` を基本とし、同じ意味には同じアイコンを使う。
- アイコンだけのボタンには `tooltip` とセマンティックラベルを付ける。
- 標準サイズは本文内 `20`、操作 `24`、強調表示 `32` を目安にする。
- グラフや評価は色だけに依存せず、値、ラベル、形状を併記する。
- AI の提案は確定情報と視覚的・文言的に区別し、ユーザーが採用するまで設定値を自動変更しない。

## 9. 文言

- 短く、具体的な日本語を使う。
- 画面タイトルと名詞は体言止め、操作は動詞で統一する。
- 専門用語は RC ユーザーに一般的な表記を優先し、同じ概念の表記を揺らさない。
- エラーには「起きたこと」「ユーザーができること」を含め、内部例外や実装用語をそのまま出さない。
- AI の回答は断定しすぎず、前提、影響、確認方法を示す。

## 10. アクセシビリティ

- タップ領域は最低 `48 x 48` dp。
- 通常テキストは WCAG AA（4.5:1）、大きな文字と UI 部品は 3:1 以上のコントラストを確保する。
- 文字拡大で切れないこと。横幅が不足する場合は省略より折り返しを優先する。ただし識別可能な短い ID は例外。
- 色、位置、アイコンだけで状態を伝えず、テキストまたは Semantics を併用する。
- フォーカス順は視覚順と一致させ、キーボード操作でも主要機能を完了できるようにする。
- 動きは状態変化の理解を助ける場合だけ使い、点滅や不要な連続アニメーションは避ける。

## 11. 実装例

推奨:

```dart
final theme = Theme.of(context);
final colors = theme.colorScheme;

Card(
  child: Padding(
    padding: const EdgeInsets.all(20),
    child: Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Text('フロント', style: theme.textTheme.titleMedium),
        const SizedBox(height: 8),
        Text(
          'ダンパーポジション 3',
          style: theme.textTheme.bodyMedium?.copyWith(
            color: colors.onSurfaceVariant,
          ),
        ),
      ],
    ),
  ),
)
```

避ける:

```dart
Container(
  decoration: BoxDecoration(
    color: Colors.white,
    borderRadius: BorderRadius.circular(13),
    boxShadow: const [BoxShadow(blurRadius: 12)],
  ),
  child: const Text(
    'フロント',
    style: TextStyle(
      color: Colors.black,
      fontSize: 19,
      fontWeight: FontWeight.bold,
    ),
  ),
)
```

後者はダークテーマに追従せず、既存の文字階層、角丸、Surface、影のルールから外れます。

## 12. レビュー用チェックリスト

UI の変更時は次を確認します。

- [ ] `AppTheme` と `ColorScheme` を使い、直接色を増やしていない
- [ ] ライト／ダークの両方で情報の階層とコントラストが保たれる
- [ ] 主要アクションが 1 つに絞られ、押した結果が文言から分かる
- [ ] 余白、角丸、文字スタイルがこのガイドのスケールに沿う
- [ ] 360 dp 程度の狭い幅と 720 dp 以上の幅で破綻しない
- [ ] 文字拡大、長い日本語、空状態、エラー、読み込み中を確認した
- [ ] タップ領域、Tooltip、Semantics、キーボードフォーカスを確認した
- [ ] スクロール末尾が NavigationBar、FAB、Safe Area に隠れない
- [ ] 新しい見た目の重複は共通 Widget または Theme にまとめた
- [ ] 必要に応じて Widget Test または Golden Test を追加・更新した

## 13. ガイドラインの更新

新しいパターンを導入するときは、画面単位の例外として増やすのではなく、次の順で扱います。

1. 既存の Theme とコンポーネントで表現できないか確認する。
2. 再利用できるセマンティックな名前と役割を決める。
3. `AppTheme`、共通 Widget、必要なら `ThemeExtension` に実装する。
4. このドキュメントとテストを同じ変更で更新する。

デザイン変更は「新しい色を追加した」ではなく、「どの役割を追加・変更したか」で説明します。

## 14. テレメトリー分析

- テレメトリー系列色は `TelemetryPalette` の `ThemeExtension` を正本とし、ライト／ダークで個別にコントラストを確保する。
- 系列色は選択順に循環利用し、再生位置は `playhead`、ブレーキ側は `brake`、推定コースは `course` を使用する。色だけに頼らず凡例・ラベル・現在値を併記する。
- 分析コンテンツの最大幅は 1200 dp とする。720 dp 未満ではカードと操作群を単一カラムまたは折り返し配置にし、横スクロールは表データだけに限定する。
- 自動算出したラップ、コース、ベストラップは「推定」「参考値」と明示し、走行ログの既存値を確認なしに上書きしない。
