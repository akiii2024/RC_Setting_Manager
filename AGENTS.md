# RC Setting Manager — Agent instructions

## Communication

- 回答、作業報告、質問、ドキュメントは原則として日本語で記述する。
- コード上の識別子、API 名、コマンド、一般的な技術用語は無理に日本語化しない。
- 不明点が結果を大きく変えない場合は、既存実装から妥当な前提を置いて進める。
- 重要な前提を置いた場合は、完了報告に記載する。

## UI and design

- UI の新規作成・変更・レビューを行う前に、ルートの `design.md` を全文確認する。
- デザイン判断は `design.md` に従う。
- 色、文字、標準コンポーネントの実装上の正本は `lib/app/app_theme.dart` とする。
- `images/` 配下の旧グリーン基調のスクリーンショットをデザイン基準にしない。
- ライトテーマとダークテーマの両方を考慮する。
- デザインルールを変更した場合は、実装と `design.md` を同時に更新する。
- 同じ UI パターンを複数画面へ追加する場合は、共通 Widget または Theme への集約を検討する。

## Flutter and Dart

- 既存のレイヤー構成と命名規則を優先し、必要のない大規模な再構成を行わない。
- 新しい依存パッケージを追加する前に、既存の Flutter / Dart API で実現できないか確認する。
- 本番用依存パッケージの追加は、理由と影響を説明してユーザーの了承を得る。
- 変更した Dart ファイルには `dart format` を適用する。
- Dart コードを変更した場合は、変更範囲に応じて `flutter analyze` と関連テストを実行する。
- バグ修正では、可能な限り再現テストまたは回帰テストを追加する。
- テストを実行できなかった場合は、理由と未確認範囲を明記する。

## Data and security

- API キー、認証情報、個人情報をソースコードやログへ追加しない。
- `.env`、Firebase 設定、認証関連ファイルを変更する場合は、公開範囲を確認する。
- 永続化データの形式を変更する場合は、既存データとの互換性を考慮する。
- データ削除や互換性を壊す変更は、影響範囲を確認してから実施する。

## Working with existing changes

- ユーザーによる未コミットの変更を保持し、依頼と無関係なファイルを変更しない。
- 生成ファイルやプラットフォーム固有ファイルを、目的なく直接編集しない。
- リファクタリングは依頼達成に必要な範囲へ限定する。

## Completion report

完了時は簡潔に次を報告する。

- 変更した内容
- 主な変更ファイル
- 実行した検証
- 残っている注意点や未確認事項
