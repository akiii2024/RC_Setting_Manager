{
  "name": "rc-setting-manager-functions",
  "description": "Cloud Functions for RC Setting Manager",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "eslint .",
    "test": "node --test test/*.test.js",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "eslint": "^10.8.0"
  },
  "private": true,
  "overrides": {
    "uuid": "^11.1.1"
  }
}
